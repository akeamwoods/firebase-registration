export type RegistrationValues = {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  password: string;
  confirmPassword: string;
};

export type User = {
  id: string;
  avatar: string;
  firstName: string;
  lastName: string;
  email: string;
  dateOfBirth: string;
};
